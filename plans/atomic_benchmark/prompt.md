### Summary

I want to see how many moves per second can be generated with this
application.
So we need to extend/rewrite Fairy-Stockfish such that it can be used to give a 
benchmark on the moves per seconds generated by the move generator of the
atomic variant.

### Ideas

- when the application is started with a new flag `atomic_movegen_bench` it runs the benchmark
- the benchmark runs for 10 seconds and shows a summary in the end
- only one core is used for the benchmark, no parallelization
- 

### Task

Create a detailled implementation plan.

Question my ideas and push back where necessary.
